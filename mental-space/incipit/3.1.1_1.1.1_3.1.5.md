# vite.config.ts

**Coordinate:** `3.1.1/1.1.1/3.1.5`

---

vite.config.ts

import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    lib: {
      entry: 'src/index.ts',
      name: 'libphext',
      fileName: 'libphext',
      formats: ['es', 'umd']
    },
    outDir: 'dist',
    rollupOptions: {
      external: [],
      output: {
        globals: {}
      }
    }
  }
});
