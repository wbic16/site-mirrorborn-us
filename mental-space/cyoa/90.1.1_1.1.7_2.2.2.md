# examples/fib_module.js

**Coordinate:** `9.1.1/1.1.7/2.2.2`

---

examples/fib_module.js

/* fib module */
export function fib(n)
{
    if (n <= 0)
        return 0;
    else if (n == 1)
        return 1;
    else
        return fib(n - 1) + fib(n - 2);
}
